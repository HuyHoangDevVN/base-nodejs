"use strict";

const COHORT_SORT_FIELDS = Object.freeze(["created_at", "name", "code"]);
const COHORT_DEFAULT_SORT = "created_at";
const COHORT_DEFAULT_ORDER = "DESC";

module.exports = {
  COHORT_SORT_FIELDS,
  COHORT_DEFAULT_SORT,
  COHORT_DEFAULT_ORDER,
};
